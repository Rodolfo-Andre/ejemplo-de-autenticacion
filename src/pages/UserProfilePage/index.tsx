import { UserProfile, ContentCenter } from "../../components";

const UserProfilePage = () => {
  return (
    <ContentCenter>
      <UserProfile />
    </ContentCenter>
  );
};

export default UserProfilePage;
